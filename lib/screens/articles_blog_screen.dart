import 'package:flutter/material.dart';

class ArticlesBlogScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Экран блога'),
    );
  }
}
