import 'package:flutter/material.dart';

class CyclesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Экран готовых курсов'),
    );
  }
}
